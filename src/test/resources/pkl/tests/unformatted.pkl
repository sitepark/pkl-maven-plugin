module com.sitepark.maven.plugins.pkl.unformatted

        local example = new Dynamic {
  foo = "bar"
        }

local example2 = new Dynamic {
  foo = "bar"
}

local example3 = new Dynamic {
  foo =
    "bar"
}
